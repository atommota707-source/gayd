{
  "scripts": {
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-3000}",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "devDependencies": {
    "vite": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
